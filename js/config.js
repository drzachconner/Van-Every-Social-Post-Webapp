// API base URL — empty string means same-origin (served by Modal backend)
// Change this to the full backend URL for cross-origin deployment
const API_BASE_URL = "";

const PLATFORM_LIMITS = {
  facebook: 63206,
  instagram: 2200,
  linkedin: 3000,
  pinterest: 800,
  tiktok: 2200,
  twitter: 280,
  youtube: 5000,
};
